export const renderFilterBar = () => `
    <section class="filters-bar">
        <select id="filter-regiao"><option value="Todas">Região (Todas)</option></select>
        <select id="filter-estado"><option value="Todos">Estado (Todos)</option></select>
        <select id="filter-grupo"><option value="Todos">Grupo (Todos)</option></select>
    </section>
`;